# Runtime requirements are provided by Home Assistant itself (requests,
# voluptuous). These are only needed to run the test suite / linters locally.
homeassistant>=2024.4.0
pytest>=8.0.0
requests>=2.31.0
